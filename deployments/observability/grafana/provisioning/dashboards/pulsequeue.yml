apiVersion: 1

providers:
  - name: PulseQueue
    orgId: 1
    folder: PulseQueue
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
